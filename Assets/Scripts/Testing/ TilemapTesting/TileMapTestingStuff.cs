using UnityEngine;
using System.Collections;
using UnityEngine.Tilemaps;
using System.Collections.Generic;

public class TileMapTestingStuff : MonoBehaviour
{

    public Tilemap tilemap;

    private List<List<int>> tilemapMatrix;

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    List<List<int>> TilemapToMatrix(Tilemap aTilemap)
    {
        //for every tile in tilemap check if there is a tile if so add a 1
        //if not add a 0

        return null;
    }
}
